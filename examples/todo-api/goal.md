# Goal

Create a simple Todo API with FastAPI. Support GET, POST, PUT, DELETE operations
on the `/todos` endpoint. Include a SQLite database.

This file is planning-goal text only. For an executable evidence-gate fixture,
see [`../build-week-demo/`](../build-week-demo/).
